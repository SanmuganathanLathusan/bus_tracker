import 'package:flutter/material.dart';

class Etickets extends StatelessWidget {
  const Etickets({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("E ticket")),
      body: const Center(child: Text("News Page Content Here")),
    );
  }
}

