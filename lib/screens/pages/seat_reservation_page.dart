import 'package:flutter/material.dart';

class SeatReservation extends StatelessWidget {
  const SeatReservation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Seat")),
      body: const Center(child: Text("News Page Content Here")),
    );
  }
}

